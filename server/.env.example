PORT=4000
CLIENT_ORIGIN=http://localhost:5173

# Local development only. Use a dedicated non-root database user in production.
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your-local-mysql-password
DB_NAME=bloom_borrow

JWT_SECRET=replace-with-a-unique-local-secret-of-at-least-32-characters
CSRF_SECRET=replace-with-a-different-local-secret-of-at-least-32-characters
JWT_EXPIRES_IN=8h
